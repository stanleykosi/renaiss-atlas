{
  "name": "renaiss-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Renaiss Atlas turns collector-market signals into source-aware next actions.",
  "type": "module",
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=20.19.0",
    "pnpm": ">=9.15.0"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "pnpm --filter @renaiss/web test:e2e",
    "db:generate": "pnpm --filter @renaiss/db db:generate",
    "db:migrate": "pnpm --filter @renaiss/db db:migrate",
    "db:seed": "pnpm --filter @renaiss/db db:seed",
    "jobs:sync:renaiss": "pnpm --filter @renaiss/worker jobs:sync:renaiss",
    "jobs:sync:gacha": "pnpm --filter @renaiss/worker jobs:sync:gacha",
    "jobs:sync:external": "pnpm --filter @renaiss/worker jobs:sync:external",
    "jobs:score": "pnpm --filter @renaiss/worker jobs:score",
    "jobs:bundles": "pnpm --filter @renaiss/worker jobs:bundles",
    "jobs:intents": "pnpm --filter @renaiss/worker jobs:intents",
    "discord:register": "pnpm --filter @renaiss/discord discord:register",
    "security:scan": "node scripts/security-scan.mjs",
    "screenshots": "CAPTURE_SCREENSHOTS=true playwright test --config playwright.config.ts apps/web/tests/screenshots.spec.ts",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "drizzle-kit": "^0.30.1",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vitest": "^2.1.8"
  }
}
