# Screenshots

Run `pnpm screenshots` locally to refresh product screenshots from the official API flow.

Expected files:

- `market.png`
- `card-detail.png`

The screenshot spec is skipped unless `CAPTURE_SCREENSHOTS=true`.
