import { SurfacePlaceholder } from "@/components/surface-placeholder";

export default function BundlesPage() {
  return (
    <SurfacePlaceholder
      eyebrow="Bundles"
      title="Bundle Explorer"
      emptyState="Bundle detection is deferred until deterministic scoring and source-backed card records exist."
    />
  );
}
