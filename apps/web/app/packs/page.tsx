import { SurfacePlaceholder } from "@/components/surface-placeholder";

export default function PacksPage() {
  return (
    <SurfacePlaceholder
      eyebrow="Packs"
      title="Pack Momentum"
      emptyState="Pack intervals will be labeled as observed public activity, never official odds."
    />
  );
}
