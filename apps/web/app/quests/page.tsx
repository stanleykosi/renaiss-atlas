import { SurfacePlaceholder } from "@/components/surface-placeholder";

export default function QuestsPage() {
  return (
    <SurfacePlaceholder
      eyebrow="Quests"
      title="Quest Lab"
      emptyState="Quest fixtures are not loaded yet. Future quests will cite the source records and scores that produced them."
    />
  );
}
